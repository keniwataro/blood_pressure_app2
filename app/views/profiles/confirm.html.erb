<div class="row justify-content-center">
  <div class="col-md-8">
    <h1 class="h2 mb-4">プロフィール更新確認</h1>

    <div class="card">
      <div class="card-header bg-warning">
        <h4 class="mb-0">以下の内容で更新します。よろしいですか？</h4>
      </div>
      <div class="card-body">
        <%= form_with model: @user, url: profile_path, method: :patch, local: true do |f| %>
          <!-- 入力値を hidden フィールドで保持 -->
          <%= f.hidden_field :name %>
          <%= f.hidden_field :email %>
          <% if @has_password_change %>
            <%= f.hidden_field :current_password %>
            <%= f.hidden_field :password %>
            <%= f.hidden_field :password_confirmation %>
          <% end %>

          <div class="row">
            <div class="col-md-6">
              <h5 class="mb-3">基本情報</h5>
              
              <div class="mb-3">
                <strong class="text-muted">ユーザーID</strong>
                <p class="h5 text-primary"><%= @user.user_id %></p>
                <div class="form-text">ユーザーIDは変更できません</div>
              </div>

              <div class="mb-3">
                <strong class="text-muted">名前</strong>
                <p class="h5"><%= @user.name %></p>
              </div>

              <div class="mb-3">
                <strong class="text-muted">メールアドレス</strong>
                <p class="h5"><%= @user.email %></p>
              </div>
            </div>

            <div class="col-md-6">
              <h5 class="mb-3">パスワード変更</h5>
              
              <% if @has_password_change %>
                <div class="alert alert-info">
                  <i class="bi bi-check-circle"></i> パスワードを変更します
                </div>
                <div class="mb-3">
                  <strong class="text-muted">新しいパスワード</strong>
                  <p class="text-muted">●●●●●●●● (入力済み)</p>
                </div>
              <% else %>
                <div class="alert alert-secondary">
                  パスワードは変更しません
                </div>
              <% end %>
            </div>
          </div>

          <hr class="my-4">

          <div class="d-grid gap-2 d-md-flex justify-content-md-between">
            <%= link_to "← 戻って修正", edit_profile_path, class: "btn btn-secondary" %>
            <%= f.submit "更新する", class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
