<div class="row justify-content-center">
  <div class="col-md-8">
    <h1 class="h2 mb-4">スタッフ登録</h1>

    <div class="card">
      <div class="card-header">
        <h4 class="mb-0">新規スタッフ情報入力</h4>
      </div>
      <div class="card-body">
        <%= form_with(model: @staff_member, url: confirm_new_medical_staff_staff_index_path, method: :post, data: { turbo: false }, html: { novalidate: true }) do |f| %>
          <% if @staff_member.errors.any? %>
            <div class="alert alert-danger">
              <h4><%= pluralize(@staff_member.errors.count, "個のエラー") %>が発生しました:</h4>
              <ul class="mb-0">
                <% @staff_member.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="mb-3">
            <%= f.label :name, "スタッフ名", class: "form-label" %>
            <span class="text-danger">*</span>
            <%= f.text_field :name, class: "form-control #{@staff_member.errors[:name].any? ? 'is-invalid' : ''}", placeholder: "例: 山田太郎" %>
            <% if @staff_member.errors[:name].any? %>
              <div class="invalid-feedback d-block">
                <%= @staff_member.errors[:name].join(', ') %>
              </div>
            <% end %>
          </div>

          <div class="mb-3">
            <%= f.label :email, "メールアドレス", class: "form-label" %>
            <span class="text-danger">*</span>
            <%= f.email_field :email, class: "form-control #{@staff_member.errors[:email].any? ? 'is-invalid' : ''}", placeholder: "例: staff@example.com" %>
            <% if @staff_member.errors[:email].any? %>
              <div class="invalid-feedback d-block">
                <%= @staff_member.errors[:email].join(', ') %>
              </div>
            <% end %>
            <div class="form-text">スタッフがログインする際に使用します（必須）</div>
          </div>

          <div class="mb-3">
            <label class="form-label"><strong>役割</strong></label>
            <span class="text-danger">*</span>
            <div class="form-text mb-2">スタッフの役割を選択してください（複数選択可、必須）</div>
            
            <% if @staff_member.errors[:base].any? { |msg| msg.include?('役割') } %>
              <div class="alert alert-danger">
                役割を少なくとも1つ選択してください
              </div>
            <% end %>
            
            <% medical_staff_roles = @roles.select(&:is_medical_staff?) %>
            <% patient_roles = @roles.reject(&:is_medical_staff?) %>
            
            <% if medical_staff_roles.any? %>
              <div class="mb-2">
                <strong class="text-success">医療従事者の役割</strong>
              </div>
              <% medical_staff_roles.each do |role| %>
                <div class="form-check">
                  <%= check_box_tag 'role_ids[]', role.id, false, 
                                    id: "role_#{role.id}", 
                                    class: "form-check-input" %>
                  <%= label_tag "role_#{role.id}", role.name, class: "form-check-label" %>
                </div>
              <% end %>
            <% end %>
            
            <% if patient_roles.any? %>
              <div class="mt-3 mb-2">
                <strong class="text-info">患者の役割</strong>
              </div>
              <% patient_roles.each do |role| %>
                <div class="form-check">
                  <%= check_box_tag 'role_ids[]', role.id, false, 
                                    id: "role_#{role.id}", 
                                    class: "form-check-input" %>
                  <%= label_tag "role_#{role.id}", role.name, class: "form-check-label" %>
                </div>
              <% end %>
            <% end %>
          </div>

          <div class="mb-3">
            <%= label_tag :permission_level, "権限レベル（医療従事者の役割用）", class: "form-label" %>
            <%= select_tag 'user[permission_level]', 
                          options_for_select([
                            ['一般', 'general'],
                            ['管理者', 'administrator']
                          ], 'general'), 
                          class: "form-select" %>
            <div class="form-text">
              <strong>管理者:</strong> スタッフの登録・編集が可能<br>
              <strong>一般:</strong> スタッフの閲覧のみ可能<br>
              <small class="text-muted">※患者の役割には権限レベルは適用されません</small>
            </div>
          </div>

          <div class="mb-3">
            <%= f.label :password, "初期パスワード", class: "form-label" %>
            <span class="text-danger">*</span>
            <%= f.password_field :password, class: "form-control #{@staff_member.errors[:password].any? ? 'is-invalid' : ''}", placeholder: "6文字以上" %>
            <% if @staff_member.errors[:password].any? %>
              <div class="invalid-feedback d-block">
                <%= @staff_member.errors[:password].join(', ') %>
              </div>
            <% end %>
            <div class="form-text">スタッフに初期パスワードを伝えてください</div>
          </div>

          <div class="mb-3">
            <%= f.label :password_confirmation, "パスワード（確認）", class: "form-label" %>
            <span class="text-danger">*</span>
            <%= f.password_field :password_confirmation, class: "form-control #{@staff_member.errors[:password_confirmation].any? ? 'is-invalid' : ''}", placeholder: "6文字以上" %>
            <% if @staff_member.errors[:password_confirmation].any? %>
              <div class="invalid-feedback d-block">
                <%= @staff_member.errors[:password_confirmation].join(', ') %>
              </div>
            <% end %>
          </div>

          <div class="alert alert-info">
            <strong>注意事項:</strong>
            <ul class="mb-0">
              <li>ユーザーIDは自動で生成されます（7桁の数字）</li>
              <li>スタッフは「<%= @hospital.name %>」に自動的に登録されます</li>
              <li>登録後、スタッフにユーザーIDとパスワードを伝えてください</li>
            </ul>
          </div>

          <div class="d-flex justify-content-between mt-4">
            <%= link_to "キャンセル", medical_staff_staff_index_path, class: "btn btn-secondary" %>
            <%= f.submit "確認画面へ", class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>