<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="h2">医療従事者ダッシュボード</h1>
  <% if @hospital %>
    <span class="badge bg-info fs-6"><%= @hospital.name %></span>
  <% end %>
</div>

<% if @hospital %>
  <!-- クイックアクション -->
  <div class="row">
    <div class="col-12">
      <div class="card">
        <div class="card-header">
          <h5 class="mb-0">クイックアクション</h5>
        </div>
        <div class="card-body">
          <div class="row g-3">
            <div class="<%= current_user.administrator_at?(@hospital) ? 'col-md-3 col-6' : 'col-md-4 col-6' %>">
              <%= link_to medical_staff_patients_path, class: "btn btn-primary w-100 quick-action-btn" do %>
                <div class="quick-action-content">
                  <i class="bi bi-people fs-1 mb-2"></i>
                  <div class="fs-5 fw-bold">患者一覧</div>
                </div>
              <% end %>
            </div>
            <div class="<%= current_user.administrator_at?(@hospital) ? 'col-md-3 col-6' : 'col-md-4 col-6' %>">
              <%= link_to new_medical_staff_patient_path, class: "btn btn-success w-100 quick-action-btn" do %>
                <div class="quick-action-content">
                  <i class="bi bi-person-plus fs-1 mb-2"></i>
                  <div class="fs-5 fw-bold">患者登録</div>
                </div>
              <% end %>
            </div>
            <div class="<%= current_user.administrator_at?(@hospital) ? 'col-md-3 col-6' : 'col-md-4 col-6' %>">
              <%= link_to medical_staff_staff_index_path, class: "btn btn-info w-100 quick-action-btn" do %>
                <div class="quick-action-content">
                  <i class="bi bi-person-badge fs-1 mb-2"></i>
                  <div class="fs-5 fw-bold">スタッフ一覧</div>
                </div>
              <% end %>
            </div>
            <% if current_user.administrator_at?(@hospital) %>
              <div class="col-md-3 col-6">
                <%= link_to new_medical_staff_staff_path, class: "btn btn-warning w-100 quick-action-btn" do %>
                  <div class="quick-action-content">
                    <i class="bi bi-person-plus-fill fs-1 mb-2"></i>
                    <div class="fs-5 fw-bold">スタッフ登録</div>
                  </div>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

<% else %>
  <div class="alert alert-warning">
    病院が登録されていません。管理者にお問い合わせください。
  </div>
<% end %>