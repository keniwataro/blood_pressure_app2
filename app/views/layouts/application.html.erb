<!DOCTYPE html>
<html>
  <head>
    <title><%= user_signed_in? && current_user.system_admin? ? "血圧管理システム - システム管理" : "血圧管理アプリ" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>
  </head>

  <body>
    <!-- ナビゲーションバー（ログイン済みの場合のみ表示） -->
    <% if user_signed_in? %>
      <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container">
          <%= link_to (user_signed_in? && current_user.system_admin? ? "血圧管理システム - システム管理" : "血圧管理アプリ"), (user_signed_in? && current_user.system_admin? ? admin_root_path : root_path), class: "navbar-brand" %>
          
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          
          <div class="collapse navbar-collapse" id="navbarNav">

            <ul class="navbar-nav me-auto">
              <% if current_user.system_admin? %>
                <!-- システム管理者用メニュー -->
                <li class="nav-item">
                  <%= link_to "ダッシュボード", admin_root_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "ユーザー一覧", admin_users_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "病院一覧", admin_hospitals_path, class: "nav-link" %>
                </li>
              <% elsif current_user.current_role_medical_staff? %>
                <!-- 医療従事者用メニュー -->
                <li class="nav-item">
                  <%= link_to "ダッシュボード", medical_staff_root_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "患者一覧", medical_staff_patients_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "スタッフ一覧", medical_staff_staff_index_path, class: "nav-link" %>
                </li>
              <% else %>
                <!-- 患者用メニュー -->
                <li class="nav-item">
                  <%= link_to "血圧記録", blood_pressure_records_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "新しい記録", new_blood_pressure_record_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "グラフ", charts_path, class: "nav-link" %>
                </li>
              <% end %>
            </ul>
            
            <ul class="navbar-nav">
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <% if current_user.current_hospital_role&.role&.is_hospital_role %>
                    <span class="text-muted small me-1"><%= current_user.current_hospital_role.hospital.name %>:</span>
                  <% end %>
                  <%= current_user.name || current_user.email %>
                  <% if current_user.system_admin? %>
                    <span class="badge bg-danger ms-1"><%= current_user.current_hospital_role.role.name %></span>
                  <% elsif current_user.current_hospital_role.present? %>
                    <% if current_user.current_role_medical_staff? %>
                      <span class="badge bg-success ms-1"><%= current_user.current_hospital_role.role.name %></span>
                    <% else %>
                      <span class="badge bg-info ms-1"><%= current_user.current_hospital_role.role.name %></span>
                    <% end %>
                  <% end %>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "プロフィール", profile_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <% if current_user.has_multiple_roles? %>
                    <li>
                      <%= link_to "役割を切り替え", root_path, class: "dropdown-item" %>
                    </li>
                    <li><hr class="dropdown-divider"></li>
                  <% end %>
                  <li><%= link_to "ログアウト", destroy_user_session_path, data: { "turbo-method": :delete }, class: "dropdown-item" %></li>
                </ul>
              </li>
            </ul>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- フラッシュメッセージ（ログイン後のみ） -->
    <% if user_signed_in? %>
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
    <% end %>

    <!-- メインコンテンツ -->
    <% if user_signed_in? %>
      <main class="container mt-4">
        <%= yield %>
      </main>

      <!-- フッター -->
      <footer class="bg-light mt-5 py-3">
        <div class="container text-center">
          <p class="mb-0">&copy; 2025 血圧管理アプリ. All rights reserved.</p>
        </div>
      </footer>
    <% else %>
      <!-- ログイン画面用（フッターなし） -->
      <%= yield %>
    <% end %>
  </body>
</html>
